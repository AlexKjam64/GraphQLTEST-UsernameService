spring:
  application:
    name: nintendo-userService
  datasource:
    url: jdbc:postgresql://localhost:5432/u2r94970203
    username: postgres
    password: password

server:
  port: 8081
  servlet:
    contextPath: /user